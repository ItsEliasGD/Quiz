@{
    Layout = "_Layout";
    ViewData["Title"] = "Quiz";
}



<div id="app" class="quiz-container mt-5">
    <div>
        <h1 class="quiz-title text-center m-xxl-5"><strong>{{quiz.Name}}</strong></h1>

          <h2 class="m-5">{{"Pregunta no." + question.Index + ": " + question.Description}}</h2>
        <div class="row justify-content-center g-3">
            <div v-for="answer in question.Answers" :class="getClasses()">
                <button class="quiz-button w-100" @@click="selectAnswer(answer)">{{ answer.Description }}</button>
            </div>
        </div>
      </div>


@*     <div class="row justify-content-center g-3">
        <div v-if="respuestas.length === 2" >

            <div class="col-6 d-flex justify-content-center">
                <button class="quiz-button w-100">RESPUESTA 1</button>
            </div>
            <div class="col-6 d-flex justify-content-center">
                <button class="quiz-button w-100">RESPUESTA 2</button>
            </div>

        </div>

       <div v-else-if="respuestas.length ===3">

            <!-- Una fila con 2 -->
            <div class="col-6 d-flex justify-content-center">
                <button class="quiz-button w-100">RESPUESTA 1</button>
            </div>
            <div class="col-6 d-flex justify-content-center">
                <button class="quiz-button w-100">RESPUESTA 2</button>
            </div>
            <!-- Esta esta centrada -->
            <div class="col-12 d-flex justify-content-center">
                <button class="quiz-button respuesta-central">RESPUESTA 3</button>
            </div>

       </div>

        <div v-else>

            <div class="col-6 d-flex justify-content-center">
                <button class="quiz-button w-100">RESPUESTA @(i + 1)</button>
            </div>
           
        </div>
    </div>
 *@</div>



@section scripts {
    <script>window.user = @Html.Raw(Json.Serialize(ViewBag.User));</script>
    <script src="~/js/Quiz.js" defer></script>
}
